import React from 'react'

function Navbar() {
  return (
    <header>
        <h1>Doctors App</h1>
        <div>
            <button>Home</button>
            <button>Add Doctor</button>
        </div>
    </header>
  )
}

export default Navbar